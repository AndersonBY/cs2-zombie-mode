name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Test and build
        run: |
          dotnet test tests/SimpleZombieMode.Tests/SimpleZombieMode.Tests.csproj -c Release --nologo
      - name: Build and validate release archive
        run: scripts/package-release.sh v0.0.5-mbsifu.0
      - uses: actions/upload-artifact@v4
        with:
          name: cs2-zombie-mode-${{ github.sha }}
          path: artifacts/
          if-no-files-found: error

